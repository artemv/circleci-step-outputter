machine:
  node:
    version: 7.0.0

test:
  post:
    - node bin/cli.js

deployment:
  master:
    branch: master
    owner: artemv8
    commands:
      - (npm run semantic-release) || true

general:
  artifacts:
    - test-output*.txt
