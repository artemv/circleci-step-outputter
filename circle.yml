machine:
  node:
    version: 8.10.0

test:
  post:
    - node bin/cli.js && mv test-output*.txt $CIRCLE_ARTIFACTS

deployment:
  master:
    branch: master
    owner: artemv
    commands:
      - (npx semantic-release) || echo "publishing failed"

